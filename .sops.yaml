# sops-nix encryption config.
# The recipient below is an age key derived from ~/.ssh/id_ed25519.pub
# (via `ssh-to-age`). Decryption uses the matching SSH private key, so no
# separate age keyfile needs to be stored. This public string is safe to commit.
keys:
  - &ryan age12rzydtlh4r9q4xa2a30rm5z4uc3yedh00pftwperl3cz75gknqns6ltf6f
creation_rules:
  - path_regex: secrets/.*\.(ya?ml|json|env)$
    key_groups:
      - age:
          - *ryan

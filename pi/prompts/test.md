---
description: Write/update and run tests for the current change
argument-hint: "[path or focus]"
---
Add or update tests covering the current change (focus: $ARGUMENTS).

1. Detect the project's test framework and conventions — don't assume.
2. Write focused tests for the changed behavior and its edge cases.
3. Run the tests and iterate until green. Report the final result.
